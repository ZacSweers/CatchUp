query RepositoriesByIds($ids: [String!]!) {
  nodes(ids: $ids) {
    ... on Repository {
      name
      description
      license
      owner {
        avatarURL
        login
      }
      url
    }
  }
}
